# Изменения: Глобальный индикатор загрузки

## Что добавлено

### ✨ Глобальный индикатор загрузки

Теперь при любых асинхронных операциях отображается красивый индикатор загрузки:
- 🔄 **Автоматическое отображение** для всех AJAX-запросов
- 📝 **Автоматическое отображение** при отправке форм
- 🎨 **Красивая анимация** с полупрозрачным оверлеем и размытием фона
- ⚡ **Умное поведение** — не мигает на быстрых запросах

## Измененные файлы

### Представления
- `HomeCenter/Views/Shared/_Layout.cshtml` — добавлен HTML индикатора загрузки
- `HomeCenter/Views/Test/Result.cshtml` — улучшена кнопка обновления результатов

### Стили
- `HomeCenter/wwwroot/css/site.css` — добавлены стили для индикатора загрузки

### JavaScript
- `HomeCenter/wwwroot/js/site.js` — добавлена логика автоматического показа/скрытия индикатора

### Документация
- `docs/LOADING_INDICATOR.md` — **новый** документ с подробным описанием

## Как это работает

### Автоматически

Индикатор показывается автоматически для:

1. **Всех fetch-запросов:**
   ```javascript
   fetch('/api/data') // Индикатор показывается автоматически
   ```

2. **jQuery AJAX (если используется):**
   ```javascript
   $.ajax({ url: '/api/data' }) // Индикатор показывается автоматически
   ```

3. **Отправки форм:**
   ```html
   <form method="post">
       <button type="submit">Отправить</button>
       <!-- Индикатор показывается при submit -->
   </form>
   ```

4. **Навигации через Pjax:**
   - При клике на ссылки в меню индикатор показывается автоматически

### Вручную

Можно управлять индикатором программно:

```javascript
// Показать
LoadingIndicator.show();

// Скрыть
LoadingIndicator.hide();

// Показать немедленно (без задержки)
LoadingIndicator.show(true);

// Сбросить состояние
LoadingIndicator.reset();
```

## Визуальные особенности

### 🎨 Дизайн

- **Полупрозрачный оверлей** — затемняет фон (rgba(0, 0, 0, 0.5))
- **Размытие фона** — backdrop-filter: blur(3px)
- **Белая карточка** — с тенью и скругленными углами
- **Анимированный спиннер** — Bootstrap spinner-border
- **Плавные анимации** — fadeIn и scaleIn

### ⚙️ Поведение

- **Задержка показа: 200мс** — не показывается для очень быстрых запросов
- **Минимальное время: 300мс** — не мигает при быстром скрытии
- **Счетчик запросов** — корректно работает при параллельных операциях
- **Автосброс** — скрывается при ошибках и таймаутах

## Дополнительные возможности

### Индикатор для кнопок

```html
<button class="btn btn-primary btn-loading">
    Загрузка...
</button>
```

### Inline индикатор

```html
<span>Обработка <span class="loading-inline"></span></span>
```

### Отключение для конкретных запросов

**Для fetch:**
```javascript
fetch('/api/data', {
    headers: { 'X-Skip-Loading': 'true' }
});
```

**Для форм:**
```html
<form data-no-loading>
    <!-- Индикатор не будет показан -->
</form>
```

## Где используется

Индикатор автоматически работает в:

✅ Админке — переключение папок, включение/отключение тестов  
✅ Импорт тестов — разбор и создание файлов  
✅ Тесты — загрузка папок, отправка ответов  
✅ Учебники — навигация по папкам  
✅ Навигация — переходы через Pjax  
✅ Результаты — обновление статуса AI обработки  

## Преимущества

✅ **Улучшенный UX** — пользователь видит, что происходит обработка  
✅ **Нет зависаний** — понятно, что приложение не зависло  
✅ **Автоматизация** — не нужно добавлять индикаторы вручную  
✅ **Универсальность** — работает со всеми типами запросов  
✅ **Красиво** — современный дизайн с анимациями  

## Обратная совместимость

✅ Все существующие AJAX-запросы продолжат работать  
✅ Не требует изменений в существующем коде  
✅ Можно отключить для конкретных запросов  
✅ Не конфликтует с jQuery и другими библиотеками  

## Настройка

Параметры можно изменить в `site.js`:

```javascript
var minDisplayTime = 300; // Минимальное время показа (мс)
var showDelay = 200;      // Задержка перед показом (мс)
```

Стили можно изменить в `site.css`:

```css
.loading-overlay {
    background-color: rgba(0, 0, 0, 0.5); /* Прозрачность */
    backdrop-filter: blur(3px);            /* Размытие */
}
```

## Дополнительная информация

Подробная документация: [docs/LOADING_INDICATOR.md](docs/LOADING_INDICATOR.md)

## Примеры использования

### Пример 1: Автоматический режим (рекомендуется)

```javascript
// Просто используйте fetch как обычно
fetch('/api/data')
    .then(r => r.json())
    .then(data => console.log(data));
// Индикатор показывается и скрывается автоматически!
```

### Пример 2: Ручное управление

```javascript
async function saveData() {
    LoadingIndicator.show();
    try {
        await fetch('/api/save', { method: 'POST', body: data });
        alert('Сохранено!');
    } finally {
        LoadingIndicator.hide();
    }
}
```

### Пример 3: Кнопка с индикатором

```javascript
button.addEventListener('click', async function() {
    this.classList.add('btn-loading');
    this.disabled = true;
    
    try {
        await doSomething();
    } finally {
        this.classList.remove('btn-loading');
        this.disabled = false;
    }
});
```
