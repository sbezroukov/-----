# Быстрый старт: Настройка AI за 5 минут

Следуйте этим шагам для быстрой настройки автоматической оценки ответов через AI.

## Шаг 1: Получите API ключ OpenRouter (2 минуты)

1. Откройте https://openrouter.ai/
2. Нажмите "Sign In" (войдите через Google/GitHub)
3. Перейдите в https://openrouter.ai/keys
4. Нажмите "Create Key"
5. Скопируйте ключ (начинается с `sk-or-v1-...`)

## Шаг 2: Создайте конфигурационный файл (1 минута)

Создайте файл `HomeCenter/appsettings.Development.json`:

```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Admin": {
    "Username": "admin",
    "Password": "admin123"
  },
  "AI": {
    "Provider": "OpenRouter",
    "ApiKey": "ВСТАВЬТЕ_СЮДА_ВАШ_КЛЮЧ",
    "Enabled": true,
    "Model": "openrouter/free",
    "BaseUrl": "https://openrouter.ai/api/v1"
  }
}
```

Замените `ВСТАВЬТЕ_СЮДА_ВАШ_КЛЮЧ` на ваш API ключ из шага 1.

## Шаг 3: Проверьте работоспособность (2 минуты)

### Вариант A: Через тестовую утилиту

```bash
cd QwenApiTest
dotnet run
```

Вы должны увидеть список работающих моделей.

### Вариант B: Через интеграционные тесты

```bash
cd HomeCenter
dotnet test --filter "Category=Integration"
```

Тест должен пройти успешно (✅ Passed).

## Шаг 4: Запустите приложение

```bash
cd HomeCenter
dotnet run
```

Откройте http://localhost:5000 и войдите с логином `admin` и паролем `admin123`.

## Готово! 🎉

Теперь при прохождении тестов с открытыми вопросами ответы будут автоматически оцениваться через AI.

## Что дальше?

- Прочитайте [docs/AI_SETUP.md](docs/AI_SETUP.md) для подробной настройки
- Посмотрите [OPENROUTER_MODELS.md](OPENROUTER_MODELS.md) для выбора других моделей
- Изучите [README.md](README.md) для общей информации о проекте

## Устранение проблем

### Ошибка "429 Too Many Requests"
- Превышен бесплатный лимит
- Подождите или пополните баланс на https://openrouter.ai/credits

### Ошибка "401 Unauthorized"
- Неверный API ключ
- Проверьте ключ на https://openrouter.ai/keys

### Ошибка "404 Model not found"
- Модель больше не доступна
- Запустите `QwenApiTest` для проверки актуальных моделей
- Измените `Model` в конфигурации на другую из списка

### AI не оценивает ответы
- Убедитесь, что `"Enabled": true` в конфигурации
- Проверьте логи приложения на наличие ошибок
- Запустите интеграционный тест для диагностики
